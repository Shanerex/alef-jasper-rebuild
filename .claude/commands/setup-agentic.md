This repo is already set up to the Agentic Engineering Playbook v2.
Use this command only when forking the structure to a new project.

Read docs/AGENTIC_ENGINEERING_PLAYBOOK.md, then create from its templates:
CLAUDE.md, docs/knowledge/ (ARCHITECTURE, DECISIONS, LEARNINGS), .claude/agents/,
.claude/skills/, .claude/commands/, and .claude/hooks/ + settings.json.
Make hook scripts executable. Report what was created and what needs human input.
