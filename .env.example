# Local demo needs no secrets. These matter only at the cheap-deploy rung.
# LLM provider for deploy (swap from Ollama). Pick one.
# ANTHROPIC_API_KEY=
# GROQ_API_KEY=
# GEMINI_API_KEY=
# Email for lead notifications (worker).
# RESEND_API_KEY=
